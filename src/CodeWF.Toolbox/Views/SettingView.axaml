<u:UrsaWindow
    x:Class="CodeWF.Toolbox.Views.SettingView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="clr-namespace:CodeWF.Core;assembly=CodeWF.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:u="https://irihi.tech/ursa"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:codewf="https://codewf.com"
    xmlns:language="clr-namespace:Localization"
    xmlns:vm="clr-namespace:CodeWF.Toolbox.ViewModels"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Width="720" WindowStartupLocation="CenterOwner"
    Height="560"
    d:DesignHeight="600"
    d:DesignWidth="500"
    mc:Ignorable="d" Padding="12"
    x:DataType="vm:SettingViewModel"
    Title="{codewf:I18n {x:Static language:SettingView.Title}}">
    <Design.DataContext>
        <vm:SettingViewModel />
    </Design.DataContext>
    <u:UrsaWindow.LeftContent>
        <Grid ColumnDefinitions="Auto">
            <StackPanel x:Name="LogoAndTitle" Grid.Row="0" Grid.Column="0" IsHitTestVisible="False"
                        VerticalAlignment="Center" Classes="h7" Margin="5 5"
                        TextElement.FontWeight="SemiBold" Orientation="Horizontal" ClipToBounds="False">
                <Viewbox Height="24" Width="24" IsHitTestVisible="False">
                    <Image Source="\Assets\logo.ico" />
                </Viewbox>
                <TextBlock Margin="8 0 0 0" Text="{codewf:I18n {x:Static language:SettingView.Title}}" />
            </StackPanel>
        </Grid>
    </u:UrsaWindow.LeftContent>
    <Grid Margin="0 40 0 0" RowDefinitions="Auto,*" RowSpacing="12">
        <TextBlock
            Margin="4 0"
            HorizontalAlignment="Left"
            TextWrapping="Wrap"
            Classes="Tertiary H6" TextTrimming="CharacterEllipsis"
            Text="{codewf:I18n {Binding #MyTab.Tag}}"
            Theme="{StaticResource TitleTextBlock}" />
        <TabControl
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            TabStripPlacement="Top"
            prism:RegionManager.RegionName="{x:Static core:RegionNames.SettingRegion}"
            Name="MyTab">
            <TabControl.Styles>
                <Style Selector="TabItem">
                    <Setter Property="CornerRadius" Value="6" />
                    <Setter Property="Padding" Value="12 8" />
                    <Setter Property="MinHeight" Value="35" />
                </Style>
            </TabControl.Styles>
        </TabControl>
    </Grid>
</u:UrsaWindow>
